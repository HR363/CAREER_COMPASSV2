import { NestFactory } from '@nestjs/core'; import { AppModule } from './src/app.module'; import { AiService } from './src/ai/ai.service'; async function bootstrap() { const app = await NestFactory.createApplicationContext(AppModule); const aiService = app.get(AiService); try { console.log(await aiService.generateCareerRecommendations({skills: 'Java, HTML', interests: 'Backend', education: 'BS CS', goals: 'CTO'}, 'fakeId')); } catch(e) { console.error('E>', e); } await app.close(); } bootstrap();
